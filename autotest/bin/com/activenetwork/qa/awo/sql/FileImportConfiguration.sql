ALTER SESSION SET CURRENT_SCHEMA=live_SK;

DECLARE
	numOfRecords NUMBER(38);

BEGIN
	--Unlocked Privilege File settings
	select count(*) into numOfRecords from D_FILE_HEADER_CFG where FILE_IMPORT_TYPE = 'UNLOCKED_PRIVILEGE';
	IF numOfRecords = 0 THEN
		INSERT INTO D_FILE_HEADER_CFG (ID, FILE_IMPORT_TYPE, FILE_COL_HEADER_TYPE, HEADER_IND, DELIMITER, FIXED_LEN_COL_IND, ACTIVE_IND, DELETED_IND) 
		    VALUES (1, 'UNLOCKED_PRIVILEGE', 'RECORD', '0', ',', '0', '1', '0');
	END IF;

    	select count(*) into numOfRecords from D_FILE_HEADER_COL_MAPPING where COL_LABEL = 'LICENCE YEAR';
	IF numOfRecords = 0 THEN
	    INSERT INTO D_FILE_HEADER_COL_MAPPING (ID, FILE_HEADER_CFG_ID, BEAN_PROP_NAME, COL_LABEL, COL_ORDER, COL_REQ_IND) 
	        VALUES (get_Sequence('D_FILE_HEADER_COL_MAPPING'), 1, 'licenseYear', 'LICENCE YEAR', 1, '1'); 
	END IF;
	
	select count(*) into numOfRecords from D_FILE_HEADER_COL_MAPPING where COL_LABEL = 'HALID';
	IF numOfRecords = 0 THEN
	    INSERT INTO D_FILE_HEADER_COL_MAPPING (ID, FILE_HEADER_CFG_ID, BEAN_PROP_NAME, COL_LABEL, COL_ORDER, COL_REQ_IND) 
			VALUES (get_Sequence('D_FILE_HEADER_COL_MAPPING'), 1, 'customerNumber', 'HALID', 2, '1');  
	END IF;
	
	select count(*) into numOfRecords from D_FILE_HEADER_COL_MAPPING where COL_LABEL = 'WMZ';
	IF numOfRecords = 0 THEN
	     INSERT INTO D_FILE_HEADER_COL_MAPPING (ID, FILE_HEADER_CFG_ID, BEAN_PROP_NAME, COL_LABEL, COL_ORDER, COL_REQ_IND) 
		     VALUES (get_Sequence('D_FILE_HEADER_COL_MAPPING'), 1, 'awardedHunt', 'WMZ', 3, '1');  
	END IF;
	
	select count(*) into numOfRecords from D_FILE_HEADER_COL_MAPPING where COL_LABEL = 'LICENCE CODE';
	IF numOfRecords = 0 THEN
		INSERT INTO D_FILE_HEADER_COL_MAPPING (ID, FILE_HEADER_CFG_ID, BEAN_PROP_NAME, COL_LABEL, COL_ORDER, COL_REQ_IND) 
		    VALUES (get_Sequence('D_FILE_HEADER_COL_MAPPING'), 1, 'privilegeCode', 'LICENCE CODE', 4, '1'); 
	END IF;
	
	COMMIT;


	-- Point Type File settings
	select count(*) into numOfRecords from D_FILE_HEADER_CFG where FILE_IMPORT_TYPE = 'POINT_BALANCES';
	IF numOfRecords = 0 THEN
		INSERT INTO D_FILE_HEADER_CFG (ID, FILE_IMPORT_TYPE, FILE_COL_HEADER_TYPE, HEADER_IND, DELIMITER, FIXED_LEN_COL_IND, ACTIVE_IND, DELETED_IND) VALUES (2, 'POINT_BALANCES', 'RECORD', '1', ',', '0', '1', '0');
	END IF;

    	select count(*) into numOfRecords from D_FILE_HEADER_COL_MAPPING where COL_LABEL = 'LICENCE YEAR' and FILE_HEADER_CFG_ID=2;
	IF numOfRecords = 0 THEN
	    INSERT INTO D_FILE_HEADER_COL_MAPPING (ID, FILE_HEADER_CFG_ID, BEAN_PROP_NAME, COL_LABEL, COL_ORDER, COL_REQ_IND) 
		VALUES (get_Sequence('D_FILE_HEADER_COL_MAPPING'),  2, 'licenseYear', 'LICENCE YEAR', 1, '1');
	END IF;
	
	select count(*) into numOfRecords from D_FILE_HEADER_COL_MAPPING where COL_LABEL = 'HALID' and FILE_HEADER_CFG_ID=2;
	IF numOfRecords = 0 THEN
	    INSERT INTO D_FILE_HEADER_COL_MAPPING (ID, FILE_HEADER_CFG_ID, BEAN_PROP_NAME, COL_LABEL, COL_ORDER, COL_REQ_IND) 
		VALUES (get_Sequence('D_FILE_HEADER_COL_MAPPING'), 2, 'customerNumber', 'HALID', 2, '1');
	END IF;
	
	select count(*) into numOfRecords from D_FILE_HEADER_COL_MAPPING where COL_LABEL = 'SPECIES' and FILE_HEADER_CFG_ID=2;
	IF numOfRecords = 0 THEN
	    INSERT INTO D_FILE_HEADER_COL_MAPPING (ID, FILE_HEADER_CFG_ID, BEAN_PROP_NAME, COL_LABEL, COL_ORDER, COL_REQ_IND) 
		VALUES (get_Sequence('D_FILE_HEADER_COL_MAPPING'), 2, 'pointTypeCode', 'SPECIES', 3, '1');  
	END IF;
	
	select count(*) into numOfRecords from D_FILE_HEADER_COL_MAPPING where COL_LABEL = 'POOL STATUS' and FILE_HEADER_CFG_ID=2;
	IF numOfRecords = 0 THEN
		INSERT INTO D_FILE_HEADER_COL_MAPPING (ID, FILE_HEADER_CFG_ID, BEAN_PROP_NAME, COL_LABEL, COL_ORDER, COL_REQ_IND) 
			VALUES (get_Sequence('D_FILE_HEADER_COL_MAPPING'), 2, 'pointBalanceAmount', 'POOL STATUS', 4, '1');
	END IF;
	

	select count(*) into numOfRecords from D_FILE_HEADER_COL_MAPPING where COL_LABEL = 'POOL STATUS AS OF' and FILE_HEADER_CFG_ID=2;
	IF numOfRecords = 0 THEN
		INSERT INTO D_FILE_HEADER_COL_MAPPING (ID, FILE_HEADER_CFG_ID, BEAN_PROP_NAME, COL_LABEL, COL_ORDER, COL_REQ_IND) 
			VALUES (get_Sequence('D_FILE_HEADER_COL_MAPPING'), 2, 'pointBalanceAsOf', 'POOL STATUS AS OF', 5, '0'); 
	END IF;

	select count(*) into numOfRecords from D_FILE_HEADER_COL_MAPPING where COL_LABEL = 'APPLICATION HISTORY' and FILE_HEADER_CFG_ID=2;
	IF numOfRecords = 0 THEN
		INSERT INTO D_FILE_HEADER_COL_MAPPING (ID, FILE_HEADER_CFG_ID, BEAN_PROP_NAME, COL_LABEL, COL_ORDER, COL_REQ_IND) 
			VALUES (get_Sequence('D_FILE_HEADER_COL_MAPPING'), 2, 'trackingType', 'APPLICATION HISTORY', 6, '0');  
	END IF;

	select count(*) into numOfRecords from D_FILE_HEADER_COL_MAPPING where COL_LABEL = 'COMMENT' and FILE_HEADER_CFG_ID=2;
	IF numOfRecords = 0 THEN
		INSERT INTO D_FILE_HEADER_COL_MAPPING (ID, FILE_HEADER_CFG_ID, BEAN_PROP_NAME, COL_LABEL, COL_ORDER, COL_REQ_IND) 
			VALUES (get_Sequence('D_FILE_HEADER_COL_MAPPING'), 2, 'icomment', 'COMMENT', 7, '0');  
	END IF;

	COMMIT;

END;
