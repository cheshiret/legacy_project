package com.activenetwork.qa.awo.supportscripts.qasetup.unused;

import com.activenetwork.qa.awo.util.AwoDatabase;

public class RolePermissionSetup {
	static AwoDatabase db = (AwoDatabase) AwoDatabase.getInstance();
	
	/**
	 * This method will assign particular permissions to a role
	 * @param roleName
	 * @param featureName - either exact the name or pattern
	 * @param application - the application like "Call Manager"
	 * @param schema
	 */
	public static void assignPermissionsToRole(String roleName,String featureName, String application, String schema) {
		db.resetSchema(schema);
		
//		String app_id,role_id;
//		List<String> feat_id=new ArrayList<String>();
		
	}
}
